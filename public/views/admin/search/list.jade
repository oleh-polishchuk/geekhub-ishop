.panel.panel-default
    .panel-heading List
    .panel-body
        div(ng-if="!products")
            .alert.alert-warning
                a.close(href='#', data-dismiss='alert', aria-label='close') ×
                strong Warning!
                |  No product found.

        h3 Search query: "{{keyword}}"

        form.form-inline(role='form')
            .form-group
                input.form-control(ng-model='keyword', type='text')
            a.btn.btn-default(href='#/api/search?name={{keyword}}' , role='button') Search
        hr
        .row
            .col-sm-3.col-md-3(ng-repeat="key in products", ng-if="products")
                .thumbnail
                    a(href='#/api/product/{{key._id}}')
                        img(src='{{key.url}}', alt='')
                    .caption
                        h3 {{key.name}}
                        p {{key.description}}
                        p
                            ngcart-addtocart(id='{{key._id}}' name='{{key.name}}', price='10.99', quantity='1', quantity-max='5') Add to Cart